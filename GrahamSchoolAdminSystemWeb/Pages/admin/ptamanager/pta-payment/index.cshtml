@page
@model GrahamSchoolAdminSystemWeb.Pages.admin.ptamanager.pta_payment.IndexModel
@{
    ViewData["Title"] = "PTA Payments History";
}
@section Sheets {
    <link rel="stylesheet" href="~/css/shared-components.css" />
    <link rel="stylesheet" href="~/css/admin-pages.css" />
    <link href="~/lib/sweetalert2/sweetalert2.min.css" rel="stylesheet" />
    <partial name="_DatatablesSheetFiles" />
}
<div class="page-header">
    <div>
        <h1>
            <i class="bi bi-list-check"></i> PTA Payment History
        </h1>
        <p class="text-white-50">Manage PTA Payments</p>
    </div>
    <div class="header-actions">
        <div class="btn-group">
            <a asp-page="MakePTAPayment" class="btn btn-primary">Record Payment</a>
        </div>
    </div>
</div>
<div class="page-content">
    <!-- Success Alert -->
    @if (!string.IsNullOrEmpty(TempData["Success"]?.ToString()))
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>
            <strong>Success!</strong> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Error Alert -->
    @if (!string.IsNullOrEmpty(TempData["Error"]?.ToString()))
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-circle-fill me-2"></i>
            <strong>Error!</strong> @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    <!-- Filters -->
    <div class="row mb-4">
        <div class="col-md-3">
            <label for="term" class="form-label">Term</label>
            <select class="form-select" id="term" asp-items="@Model.Selections.Terms">
                <option value="">---Select Term---</option>
            </select>
        </div>
        <div class="col-md-3">
            <label for="session" class="form-label">Academic Session</label>
            <select class="form-select" id="session" asp-items="@Model.Selections.AcademicSession">
                <option value="">---Select Session---</option>
            </select>
        </div>
        <div class="col-md-3">
            <label for="schoolclass" class="form-label">Class</label>
            <select class="form-select" id="schoolclass" asp-items="@Model.Selections.SchoolClasses">
                <option value="">---Select Class---</option>
            </select>
        </div>
        <div class="col-md-3">
            <label for="subclass" class="form-label">Sub Class</label>
            <select class="form-select" id="subclass" asp-items="@Model.Selections.SubClass">
                <option value="">---Select Sub Class---</option>
            </select>
        </div>
    </div>

    <!-- DataTable Container -->
    <div class="datatable-container">
        <table id="ptaPaymentsTable" class="table table-hover table-sm">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Full Name</th>
                    <th>Reg. Number</th>
                    <th>Academic Session</th>
                    <th>State</th>
                    <th>Status</th>
                    <th>PTA Fees</th>
                    <th>Amount Paid</th>
                    @* <th>Balance</th> *@
                    <th>Created Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <partial name="_DatatablesScriptsFiles" />
    <script src="~/lib/sweetalert2/sweetalert2.all.min.js"></script>
    <script src="~/lib/jquery.blockui/jquery.blockui.min.js"></script>
    <script src="~/js/appmain.js" asp-append-version="true"></script>
    <script>
        $(document).ready(function(){
            let tabledata = null;

            // Initialize DataTable
            if ($("#ptaPaymentsTable").length) {
                tabledata = $('#ptaPaymentsTable').DataTable({
                    serverSide: true,
                    processing: true,
                    searching: true,
                    paging: true,
                    info: true,
                    responsive: true,
                    ajax: {
                        url: "/home/GetPTAPayments",
                        type: "POST",
                        dataType: 'json',
                        data: function (d) {
                            // Get filter values
                            const term = $('#term').val();
                            const session = $('#session').val();
                            const schoolclass = $('#schoolclass').val();
                            const subclass = $('#subclass').val();

                            // Prepare DataTable form data with filters
                            const formData = new FormData();
                            formData.append('draw', d.draw);
                            formData.append('start', d.start || 0);
                            formData.append('length', d.length || 10);
                            formData.append('search[value]', d.search?.value || '');
                            if (d.order && d.order.length > 0) {
                                formData.append('order[0][column]', d.order[0].column);
                                formData.append('order[0][dir]', d.order[0].dir);
                            }
                            formData.append('term', term);
                            formData.append('session', session);
                            formData.append('schoolclass', schoolclass);
                            formData.append('subclass', subclass);

                            return formData;
                        },
                        processData: false,
                        contentType: false
                    },
                    columns: [
                        {
                            data: null,
                            orderable: false,
                            searchable: false,
                            autoWidth: true,
                            render: function (data, type, row, meta) {
                                return meta.settings._iDisplayStart + meta.row + 1;
                            }
                        },
                        { data: "name", autoWidth: true },
                        { data: "regnumber", autoWidth: true },
                        {
                            data: null, autoWidth: true,
                            render: function (data, type, row) {
                                return `
                                    <div class=''>
                                        <span class="bg-primary badge">${row.session}</span>
                                        <small>${row.term} - ${row.schoolclass}</small>
                                    </div>
                                `;
                            }
                        },
                        { data: "state", autoWidth: true },
                        { data: "status", autoWidth: true },
                        { data: "fees", autoWidth: true },
                        { data: "amount", autoWidth: true },
                        // { data: "balance", autoWidth: true },
                        {
                            data: "createdate",
                            autoWidth: true,
                            render: function (data, type, row) {
                                if (data != null) {
                                    let date = new Date(data);
                                    return date.toLocaleString('en-US', { year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit' });
                                }
                                return "N/A";
                            }
                        },
                        {
                            data: null,
                            orderable: false,
                            searchable: false,
                            render: function (data, type, row) {
                                return `
                                    <div class="dropdown">
                                        <button class="btn btn-dark btn-sm" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                            <i class="bi bi-three-dots-vertical me-1"></i>
                                        </button>
                                        <ul class="dropdown-menu">
                                            <li><a class="dropdown-item" href="/admin/ptamanager/pta-payment/view-pta-payment-detail?id=${row.id}"><i class="bi bi-eye"></i> View Details</a></li>
                                            <li><a class="dropdown-item" href="/admin/ptamanager/pta-payment/pta-payment-receipt?paymentId=${row.id}" target="_blank"><i class="bi bi-printer"></i> Print Receipt</a></li>
                                            <li><button class="dropdown-item btn-delete-single" data-id="${row.id}"><i class="bi bi-trash"></i> Delete</button></li>
                                        </ul>
                                    </div>
                                `;
                            }
                        }
                    ]
                });
            }


            // Single Delete
            $(document).on('click', '.btn-delete-single', function () {
                const id = $(this).data('id');

                const swalWithBootstrapButtons = Swal.mixin({
                    customClass: {
                        confirmButton: "btn btn-danger rounded-pill btn-sm",
                        cancelButton: "btn btn-secondary rounded-pill btn-sm me-2"
                    },
                    buttonsStyling: false
                });

                swalWithBootstrapButtons.fire({
                    title: "Delete Confirmation",
                    text: 'Are you sure you want to remove this record?',
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonText: "Yes, Delete",
                    cancelButtonText: "No, Cancel",
                    reverseButtons: true
                }).then((result) => {
                    if (result.isConfirmed) {
                        $.ajax({
                            type: "DELETE",
                            url: `/api/v1/ptapayment/${id}`,
                            contentType: 'application/json',
                            beforeSend: function () {
                                $.blockUI({
                                    message: '<div class="spinner-border text-light" role="status"><span class="visually-hidden">Loading...</span></div>',
                                    css: {
                                        backgroundColor: 'transparent',
                                        border: 'none'
                                    },
                                    overlayCSS: {
                                        backgroundColor: '#000',
                                        opacity: 0.6
                                    }
                                });
                            },
                            success: function (response) {
                                $.unblockUI();
                                if (response.success) {
                                    swalWithBootstrapButtons.fire({
                                        title: 'Success!',
                                        text: response.message,
                                        icon: 'success'
                                    });
                                    tabledata.ajax.reload(null, false);
                                } else {
                                    Swal.fire({
                                        icon: 'error',
                                        title: 'Error',
                                        text: response.message
                                    });
                                }
                            },
                            error: function () {
                                $.unblockUI();
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Error',
                                    text: 'An error occurred while deleting the record.'
                                });
                            }
                        });
                    }
                });
            });

            // Filter Logic - Only apply when all filters are selected
            $('#term, #session, #schoolclass, #subclass').on('change', function () {
                const term = $('#term').val();
                const session = $('#session').val();
                const schoolclass = $('#schoolclass').val();
                const subclass = $('#subclass').val();

                // Only reload table if all filters are selected
                if (term && session && schoolclass && subclass) {
                    // Reset pagination to first page when filtering
                    tabledata.page(0).draw('page');
                } else {
                    // If filters are cleared, also show all records
                    if (!term && !session && !schoolclass && !subclass) {
                        tabledata.page(0).draw('page');
                    }
                }
            });

            // Clear filters function
            function clearFilters() {
                $('#term').val('');
                $('#session').val('');
                $('#schoolclass').val('');
                $('#subclass').val('');
                tabledata.ajax.reload();
            }
        });
    </script>
}
