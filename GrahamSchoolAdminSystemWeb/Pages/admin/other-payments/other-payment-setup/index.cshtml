@page
@model GrahamSchoolAdminSystemWeb.Pages.admin.other_payments.other_payment_setup.indexModel
@{
    ViewData["Title"] = "Other Fees Setup - Graham School Admin System";
}

@section Sheets{
    <link rel="stylesheet" href="~/css/shared-components.css" />
    <link rel="stylesheet" href="~/css/admin-pages.css" />
    <partial name="_DatatablesSheetFiles" />
    <link href="~/lib/sweetalert2/sweetalert2.min.css" rel="stylesheet" />
}

<div class="page-header">
    <div>
        <h1>
            <i class="bi bi-cash-coin"></i> Other Fees Setup Management
        </h1>
        <p class="text-white">Manage other fees for school classes and items</p>
    </div>
    <div class="header-actions">
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#otherFeesSetupModal" onclick="resetForm()">
            <i class="bi bi-plus-circle me-2"></i>Add Other Fees Setup
        </button>
    </div>
</div>

<div class="page-content">
    <!-- Success Alert -->
    @if (!string.IsNullOrEmpty(TempData["Success"]?.ToString()))
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>
            <strong>Success!</strong> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Error Alert -->
    @if (!string.IsNullOrEmpty(TempData["Error"]?.ToString()))
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-circle-fill me-2"></i>
            <strong>Error!</strong> @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- DataTable Container -->
    <div class="datatable-container">
        <table id="otherFeesSetupTable" class="table table-hover table-sm">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Term</th>
                    <th>Academic Session</th>
                    <th>Class</th>
                    <th>Payment Item</th>
                    <th>Amount</th>
                    <th>Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>

<!-- Add Other Fees Setup Modal -->
<div class="modal fade" id="otherFeesSetupModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <div>
                    <h5 class="modal-title">
                        <i class="bi bi-cash-coin me-2"></i><span id="modalTitle">Add Other Fees Setup</span>
                    </h5>
                    <small>Create new other fees set up information</small>
                </div>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form method="post">
                <div class="modal-body">
                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.SchoolClassId">
                            <i class="bi bi-building me-1"></i>School Class <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" id="schoolClassId" asp-for="OtherFeesSetupModel.SchoolClassId" required asp-items="@Model.Selections.SchoolClasses">
                            <option selected>-- Select a class --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.SchoolClassId"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.SessionId">
                            <i class="bi bi-calendar-event me-1"></i>Academic Session <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" asp-for="OtherFeesSetupModel.SessionId" required asp-items="@Model.Selections.AcademicSession">
                            <option disabled selected>-- Select a session --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.SessionId"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.Term">
                            <i class="bi bi-hourglass-split me-1"></i>Term <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" asp-for="OtherFeesSetupModel.Term" required asp-items="@Model.Selections.Terms">
                            <option disabled selected>-- Select a term --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.Term"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.OtherPayItemId">
                            <i class="bi bi-tag me-1"></i>Payment Item <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" asp-for="OtherFeesSetupModel.OtherPayItemId" required asp-items="@Model.Selections.PaymentItems">
                            <option disabled selected>-- Select a payment item --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.OtherPayItemId"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.Amount">
                            <i class="bi bi-currency-dollar me-1"></i>Amount <span class="text-danger">*</span>
                        </label>
                        <input type="number" class="form-control" asp-for="OtherFeesSetupModel.Amount"
                               placeholder="Enter other fee amount" step="0.01" min="0" required>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.Amount"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle me-1"></i>Cancel
                    </button>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-circle me-1"></i><span id="submitBtnText">Save</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Other Fees Setup Modal -->
<div class="modal fade" id="otherFeesSetupEditModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <div>
                    <h5 class="modal-title">
                        <i class="bi bi-cash-coin me-2"></i><span id="modalTitleEdit">Edit Other Fees Setup</span>
                    </h5>
                    <small>Update other fees set up information</small>
                </div>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form method="post">
                <div class="modal-body">
                    <input type="hidden" asp-for="OtherFeesSetupModel.Id" />

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.SchoolClassId">
                            <i class="bi bi-building me-1"></i>School Class <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" id="schoolClassIdEdit" asp-for="OtherFeesSetupModel.SchoolClassId" required asp-items="@Model.Selections.SchoolClasses">
                            <option selected>-- Select a class --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.SchoolClassId"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.SessionId">
                            <i class="bi bi-calendar-event me-1"></i>Academic Session <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" asp-for="OtherFeesSetupModel.SessionId" required asp-items="@Model.Selections.AcademicSession">
                            <option disabled selected>-- Select a session --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.SessionId"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.Term">
                            <i class="bi bi-hourglass-split me-1"></i>Term <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" asp-for="OtherFeesSetupModel.Term" required asp-items="@Model.Selections.Terms">
                            <option disabled selected>-- Select a term --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.Term"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.OtherPayItemId">
                            <i class="bi bi-tag me-1"></i>Payment Item <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" asp-for="OtherFeesSetupModel.OtherPayItemId" required asp-items="@Model.Selections.PaymentItems">
                            <option disabled selected>-- Select a payment item --</option>
                        </select>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.OtherPayItemId"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" asp-for="OtherFeesSetupModel.Amount">
                            <i class="bi bi-currency-dollar me-1"></i>Amount <span class="text-danger">*</span>
                        </label>
                        <input type="number" class="form-control" asp-for="OtherFeesSetupModel.Amount"
                               placeholder="Enter other fee amount" step="0.01" min="0" required>
                        <span class="invalid-feedback" asp-validation-for="OtherFeesSetupModel.Amount"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle me-1"></i>Cancel
                    </button>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-circle me-1"></i><span id="submitBtnTextEdit">Update</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts{
    <partial name="_ValidationScriptsPartial" />
    <partial name="_DatatablesScriptsFiles" />
    <script src="~/lib/sweetalert2/sweetalert2.all.min.js"></script>
    <script src="~/js/appmain.js"></script>
    <script>
        var otherFeesSetupTable;
        var editingId = null;

        $(function () {
            // Initialize DataTable
            otherFeesSetupTable = $("#otherFeesSetupTable").DataTable({
                serverSide: true,
                processing: true,
                searching: true,
                paging: true,
                info: true,
                responsive: true,
                language: {
                    processing: '<div class="spinner-border text-primary" role="status"><span class="visually-hidden">Loading...</span></div>'
                },
                ajax: {
                    url: "/home/GetOtherFeesSetup",
                    type: "POST",
                    data: function (d) {
                        d.searchTerm = $(".dataTables_filter input").val();
                    }
                },
                columns: [                
                    {
                        data: null, orderable: false, searchable: false, autoWidth: true,
                        render: function (data, type, row, meta) {
                            return meta.settings._iDisplayStart + meta.row + 1;
                        }
                    },
                    { data: "term", "autoWidth": true, },
                    { data: "sessionname", "autoWidth": true, },
                    { data: "classname", "autoWidth": true, },
                    { data: "itemname", "autoWidth": true, },
                    {
                        data: "amount", "autoWidth": true,
                        render: function (data) {
                            return data;
                        }
                    },
                    {
                        "data": "createdate", "autoWidth": true,
                        render: function (data, type, row) {
                            if (data != null) {
                                var hours = new Date(data).getHours()
                                let ap = hours >= 12 ? 'pm' : 'am';
                                return data = data.toLocaleString('YYYY-MM-dd').slice(0, 19).replace('T', ' ') + ' ' + ap;
                            } else {
                                return "null";
                            }
                        }
                    },
                    {
                        data: "id", "autoWidth": true,
                        render: function (data) {
                              return `
                                <div class="">
                                  <button type="button" class="btn btn-primary btn-sm" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="bi bi-three-dots-vertical"></i>
                                  </button>
                                  <ul class="dropdown-menu">
                                    <li><button type="button" class="dropdown-item" onclick="editOtherFeesSetup(${data})" title="Edit">
                                        <i class="bi bi-pencil-square"></i> Edit
                                    </button></li>
                                    <li><button type="button" class="dropdown-item" onclick="deleteOtherFeesSetup(${data})"><i class="bi bi-trash me-2"></i>Delete</button></li>
                                  </ul>
                                </div>
                                `;
                        },
                        orderable: false,
                        searchable: false
                    }
                ],
                pageLength: 10,
                lengthMenu: [[10, 25, 50, 100], [10, 25, 50, 100]],
                order: [[0, "desc"]],
                language: {
                    searchPlaceholder: "Search other fees setup..."
                }
            });
        });

        function resetForm() {
            editingId = null;
            document.querySelector('input[name="OtherFeesSetupModel.Id"]').value = "0";
            document.querySelector('select[name="OtherFeesSetupModel.SchoolClassId"]').value = "";
            document.querySelector('select[name="OtherFeesSetupModel.SessionId"]').value = "";
            document.querySelector('select[name="OtherFeesSetupModel.Term"]').value = "";
            document.querySelector('select[name="OtherFeesSetupModel.OtherPayItemId"]').value = "";
            document.querySelector('input[name="OtherFeesSetupModel.Amount"]').value = "";
            document.getElementById("modalTitle").textContent = "Add Other Fees Setup";
            document.getElementById("submitBtnText").textContent = "Save";
        }

        function editOtherFeesSetup(id) {
            $.ajax({
                url: "/api/v1/othersetuppayment/"+id,
                type: "GET",
                contentType: "application/json",
                success: function (data) {
                    if (data) {
                        editingId = data.id;

                        $('input[name="OtherFeesSetupModel.Id"]').val(data.id);
                        $('select[name="OtherFeesSetupModel.SchoolClassId"]').val(data.schoolClassId);
                        $('select[name="OtherFeesSetupModel.SessionId"]').val(data.sessionId);
                        $('select[name="OtherFeesSetupModel.Term"]').val(data.term);
                        $('select[name="OtherFeesSetupModel.OtherPayItemId"]').val(data.otherPayId);
                        $('input[name="OtherFeesSetupModel.Amount"]').val(data.amount);

                        $("#modalTitleEdit").text("Edit Other Fees Setup");
                        $("#submitBtnTextEdit").text("Update");

                        var editModal = new bootstrap.Modal($("#otherFeesSetupEditModal")[0]);
                        editModal.show();
                    }
                },
                error: function (xhr, status, error) {
                    console.error("Error:", error);

                    Swal.fire({
                        icon: "error",
                        title: "Error",
                        text: "Failed to load other fees setup details"
                    });
                }
            });
        }

        function deleteOtherFeesSetup(id) {
            Swal.fire({
                title: "Are you sure?",
                text: "You won't be able to revert this action if there are no related payment records!",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: "#d33",
                cancelButtonColor: "#3085d6",
                confirmButtonText: "Yes, delete it!"
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch("/api/v1/DeleteOtherFeesSetup/" + id, {
                        method: "DELETE",
                        headers: {
                            "Content-Type": "application/json"
                        }
                    })
                        .then(response => response.json())
                        .then(data => {
                            if (data.succeeded) {
                                Swal.fire({
                                    icon: "success",
                                    title: "Deleted!",
                                    text: data.message
                                }).then(() => {
                                    otherFeesSetupTable.ajax.reload();
                                });
                            } else {
                                Swal.fire({
                                    icon: "error",
                                    title: "Error",
                                    text: data.message
                                });
                            }
                        })
                        .catch(error => {
                            console.error("Error:", error);
                            Swal.fire({
                                icon: "error",
                                title: "Error",
                                text: "Failed to delete other fees setup"
                            });
                        });
                }
            });
        }
    </script>
}
